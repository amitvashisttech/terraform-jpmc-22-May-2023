```
local backend + local state ->   Your State file will genrated in the PWD ( Exuting your Terraform ) 
local backend + Remote state ->  Your State file will gerated outside of the PWD.( Any Remote Location on the same Box / Node / Server / Machine )
remote backend + Remote state -> Now your state will be stored in configured Remote Backed I.E ( S3 Or Azure Blob ) 
```
